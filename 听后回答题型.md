# 听后回答题型（显示逻辑 V2）

## 1. 目标
- `新建 -> 听后回答` 使用与 `听后选择` 一致的编辑样式（题干、题组、小题卡片）。
- 运行逻辑按“逐小题录音回答”执行，不是选择题点击选项。
- 标准默认数据固定为：`1 个单题组 + 1 个双题组（第4-5题）`。

## 2. 数据形态（当前实现约束）
- 题型主结构复用 `listening_choice`。
- 通过 `metadata.questionVariant = 'hear_answer'` 标记业务语义。
- 独立标准流程线：`listening_hear_answer.standard.v1`（展示名：听后回答标准）。
- UI 标题显示为“听后回答”。

## 3. 你描述的目标显示逻辑（按题组/小题）
### 3.1 全局介绍阶段
1. 介绍页播放介绍音频。

### 3.2 第 1 题（单题组）
1. 文案：听下面一段对话，回答第1小题，现在你有5秒钟的时间阅读这道小题。  
2. 显示：`1. who is the boy ...`
3. 步骤顺序：
   - 播放描述音频
   - 答题准备倒计时 5 秒
   - 播放小题音频
   - 开始录音

### 3.3 第 4-5 题（双题组）
1. 文案：听下面一段对话，回答第4-5小题，现在你有10秒钟的时间阅读这两道小题。  
2. 先同时显示：
   - `4. how often should the girl water ...`
   - `5. what will the speakers do next`
3. 然后逐题进入录音准备与作答：
   - 第4题：提示“下面请准备录音，听到录音提示音后，在10秒钟内开始作答”，显示第4题，开始录音
   - 第5题：提示“下面请准备录音，听到录音提示音后，在10秒钟内开始作答”，显示第5题，开始录音

## 4. 流程抽象（用于流程图配置）
- `intro`：介绍页（可带介绍音频）
- `groupPrompt`：题组说明（显示“回答第X小题/第X-Y小题”）
- `countdown`：阅读/准备倒计时（5秒或10秒）
- `playAudio(description)`：播放描述音频
- `playAudio(content)`：播放小题音频
- `record`：开始录音（听后回答专用）

## 5. 与当前系统的关系
- 编辑样式：已与听后选择统一。
- 流程绑定：听后回答不再复用“听后选择标准”，会优先命中“听后回答标准”流程线。
- 运行层：当前引擎主路径仍是听后选择链路，`record` 录音步骤需要在听后回答分支补齐执行能力。
- 本文档作为“听后回答目标交互”的基线，后续实现以此为准。

## 6. 文案/拼写修正建议
- `how oftens` 建议改为 `how often`。
- 第5题第二次展示文案应显示第5题文本，不应复用第4题文本。
